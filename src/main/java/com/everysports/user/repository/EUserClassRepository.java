package com.everysports.user.repository;

import com.everysports.user.domain.EUserClass;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EUserClassRepository extends JpaRepository<EUserClass, Long> {
}
